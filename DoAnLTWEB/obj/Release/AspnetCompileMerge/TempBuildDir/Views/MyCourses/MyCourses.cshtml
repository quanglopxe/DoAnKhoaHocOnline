
@{
    ViewBag.Title = "MyCourses";
    Layout = "~/Views/Shared/_LayoutUser.cshtml";
}

@using DoAnLTWEB.Models;
@model List<CourseCart>
<style>
    a {
        text-decoration: none;
    }
</style>
@{
    var User = Session["User"] as NguoiDung;
}
<body>
    <div style="width:100%; height: auto">
        <h2 style="text-align:center">KHÓA HỌC CỦA BẠN</h2>
        @if (Model.Count != 0)
        {
            <div class="card border-dark">
                <table style="text-align: center">
                    <thead><a href="/MyCourses/XoaMyCoursesAll" style="text-align:center" class="btn btn-danger">DELETE ALL</a></thead>
                    <tr style="background-color: lightyellow; margin:auto">
                        <th>MÃ KHÓA HỌC</th>
                        <th>TÊN KHÓA HỌC</th>
                        <th>GIẢNG VIÊN</th>
                        <th>NGÀY BẮT ĐẦU</th>
                        <th>NGÀY KẾT THÚC</th>
                        <th>ẢNH BÌA</th>
                        <th>GIÁ</th>
                    </tr>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td width="12%">@item.MaKH</td>
                            <td width="20%">@item.TenKH</td>
                            <td width="12%">@item.GiangVien</td>
                            <td width="16%">@item.NgayBD.Value.ToString("dd/MM/yyyy")</td>
                            <td width="16%">@item.NgayKT.Value.ToString("dd/MM/yyyy")</td>
                            <td width="16%"><img src="~/Content/Images/@item.Picture" width="80%" /></td>
                            @*@using (Html.BeginForm("CapNhatMyCourses", "MyCourses", new { MaKH = item.MaKH }))
                            {*@
                                <td width="22%">@String.Format("{0:0,0}", item.GiaKhoaHoc)</td>                                
                                <td><a href="/Lesson/Lesson?MaKH=@item.MaKH" class="btn btn-primary">Details</a></td>
                                <td><a href="/MyCourses/XoaMyCourses?MaKH=@item.MaKH" class="btn btn-danger">Delete</a></td>

                            @*}*@
                        </tr>

                    }
                    <tr style="background-color: lightyellow;">
                        <td colspan="3" style="color:blue; font-weight:bold; ">Tổng cộng: </td>
                        <td style="color:red; font-weight: bold;">@ViewBag.TongSoLuong</td>
                        <td></td>
                        <td></td>
                        <td style="color:red; font-weight: bold;">@String.Format("{0:0,0}", ViewBag.TongThanhTien) VNĐ</td>
                        <td></td>
                        <td></td>
                        <td></td>
                    </tr>
                </table>
                <div class="input-group">
                    <a href="/Payment/ThanhToan" class="text-center btn btn-success">THANH TOÁN</a>
                    <a href="/Course/Course" class="text-center btn btn-primary" style="position:relative;left:10px">QUAY LẠI</a>
                </div>
            </div>
        }
        else
        {
            <center><h5>Bạn không có khóa học nào cần thanh toán.</h5></center>
        }
    </div>
</body>